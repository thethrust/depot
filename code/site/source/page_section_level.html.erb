---
pageable: true
per_page: 12
---

<% fixes = { "org" => "organisation", "toolsused" => "tools used", "orgtype" => "type of organisation" } %>
<% meta_tag_list = data["ind-all"].meta[tag_name] %>

<%= partial("partials/header", :locals => { :fixes => fixes, :header_class => "" }) %>
<%= partial("partials/section_type", :locals => { :tag => "type", :tag_name => tag_name, :tag_list => meta_tag_list, :fixes => fixes }) %>