
<% fixes = { "org" => "organisation", "toolsused" => "tools used", "orgtype" => "type of organisation" } %>

<% tag_name = tag_name || "default" %>

<%= partial("partials/header", :locals => { :fixes => fixes, :header_class => "", :active_tag => tag_name }) %>
<%= partial("partials/section_default", :locals => { :slug => sluggify(tag_name), :tag => tag, :tag_name => tag_name }) %>