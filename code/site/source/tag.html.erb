---
pageable: true
per_page: 12
---

<% fixes = { "org" => "organization", "toolsused" => "tools used", "orgtype" => "type of organization" } %>
<% meta_tag_list = data["ind-all"].meta[tag_name] %>

<%= partial("partials/header", :locals => { :fixes => fixes, :header_class => "", :active_tag => tag_name }) %>
<%= partial("partials/section_type", :locals => { :tag => "type", :tag_name => tag_name, :tag_list => meta_tag_list }) %>